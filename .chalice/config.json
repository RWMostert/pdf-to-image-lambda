{
  "version": "2.0",
  "app_name": "pdf2image",
  "stages": {
    "dev": {
      "api_gateway_stage": "api",
      "lambda_memory_size": 2048,
      "lambda_timeout": 900
    }
  },
  "environment_variables": {
    "ORIGIN_BUCKET": <S3 ORIGIN BUCKET HERE>,
    "DESTINATION_BUCKET": <S3 DESTINATION BUCKET HERE>,
    "FMT": "png",
    "DPI": 300
  }
}
